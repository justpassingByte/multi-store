specVersion: "v1beta"
serviceId: "food-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "food-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
